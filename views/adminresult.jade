doctype html
html(lang='en')
  head
    title AddQuestions
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/css/bootstrap.css')
    link(rel='stylesheet', href='/css/main2.css')
    script(src='/js/jquery.js')
    script(src='/js/popper.js')
    script(src='/js/bootstrap.js')
  body
    nav.navbar.navbar-expand-sm.bg-dark.navbar-dark
      p
        a.navbar-brand(href='#') QUIZAPP
      ul.navbar-nav
        li.nav-item
          p
            a.nav-link(href='#') HOME
        li.nav-item
          p
            a.nav-link(href='#') ABOUT
        li.nav-item
          p
            a.nav-link(href='#') FAQ
    .login-block2
      .container
        .row
            if results
                table.table.table-striped
                    thead
                        td S.No.
                        td Username
                        td quizname
                        td scores
                    tbody
                        each res, i in results
                            td #{i}
                            td #{res.username}
                            td #{res.scores[0].subject}
                            td #{res.scores[0].score}
            else
                h1#addquizheading  Show results
                br
                .dropdown
                    .addquizmenu
                        if branches
                            each branch, i in branches
                                a.btn.btn-danger(href="/admin/viewscores/#{branch.branch}") #{branch.branch}
                                br